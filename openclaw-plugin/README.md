# OpenClaw Plugin (Deferred)

This directory is intentionally kept for future compatibility work.

## Status

- **Primary architecture today:** transparent proxy mode (iptables + `moat start --proxy`)
- **Plugin mode:** deferred to a future version

Use proxy mode for production deployments.
